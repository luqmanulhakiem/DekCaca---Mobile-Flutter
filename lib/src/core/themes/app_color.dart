import 'package:flutter/material.dart';

class AppColor {
  // static const primary = Color(0xFF003366);
  static const primary = Color.fromARGB(255, 13, 108, 202); //hex #0D6CCA
  // static const primary = Color(0xFF42A5F5);
  static const secondary = Color(0xFFFFFFFF);
  static const button = Color(0xFF66BB66);
  static const bar = Color(0xFF003366);
  static const border = Colors.grey;
  static const error = Colors.red;
}
